# Database Configuration
SQLALCHEMY_DATABASE_URL = "mysql+pymysql://root:admin@localhost:3315/OTDREPORTES"

# Environment
ENVIRONMENT=development

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Security
SECRET_KEY=your_secret_key_here_change_in_production

# SMTP (Gmail) Configuration
# These values are read by utils/email_service.py when sending recovery or welcome emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=josthinpaz2@gmail.com
SMTP_PASSWORD=wlamscgkcifamnyl

# Disable emails in development
DISABLE_EMAILS=true
